#!/bin/bash

# Check Minikube
if ! command -v minikube &> /dev/null
then
    echo "Minikube not found!"
    exit 1
fi

# Start Minikube
minikube start

# Show cluster info
kubectl cluster-info

# List all pods
kubectl get pods -A
